<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "SF Events planner" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="top-bar" data-topbar role="navigation">
    <ul class="title-area">
      <li class="name">
        <h1><%= link_to 'Spring Forward Events Planner', new_event_path %></h1>
      </li>
    </ul>
  <section class="top-bar-section">
    <ul class="right">
      <li class="active"><%= link_to 'Return home', root_path %></li>
    </ul>
</nav>
    <% if flash[:notice] %>
      <div data-alert class="alert-box success">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div data-alert class="alert-box alert">
        <%= flash[:alert] %>
      </div>
    <% end %>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>